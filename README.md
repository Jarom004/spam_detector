# Practica Spam
Esta es una practica de un detector de spam.

## De que trata la practica 
En esta practica se entrenó el detector de spam con datos ham y datos spam, con este entrenamiento detecta e indica si el texto o email es spam o no es spam. Aprendimos a como entrenar este modelo con datos ham y spam.

## Como?
1- Creamos la carpeta spam_detector
2- En un entorno virtual descargamos las librerias necesarias
3- Creamos 3 archivos el principal, el de la ventana emergente y el detector de spam
4- Subimos un csv con todos los ham y spam para entrenar el modelo

Utilizamos las librerias librerias:
joblib==1.5.2
numpy==2.3.4
pandas==2.3.3
python-dateutil==2.9.0.post0
pytz==2025.2
scikit-learn==1.7.2
scipy==1.16.3
six==1.17.0
threadpoolctl==3.6.0
tzdata==2025.2

## Trabajo futuro (mejores)
-Entrenar mas el modelo con texto ham y spam pero ahora en español
